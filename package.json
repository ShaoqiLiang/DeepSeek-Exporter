{
  "name": "deepseek-exporter",
  "version": "1.0.0",
  "description": "一键导出 DeepSeek 对话中的多轮问答记录",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --package",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "crx": "^5.0.1",
    "esbuild": "^0.21.5",
    "sharp": "^0.35.2",
    "typescript": "^5.4.0"
  }
}
